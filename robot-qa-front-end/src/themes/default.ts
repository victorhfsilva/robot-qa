import {borderRadius, colors, fonts, fontSize, fontWeight, lineHeight} from './tokens'

const defaultTheme = {
    borderRadius,
    colors,
    fonts,
    fontSize,
    fontWeight,
    lineHeight
}

export default defaultTheme;